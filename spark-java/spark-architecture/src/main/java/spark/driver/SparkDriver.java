/*

A driver program is an application that uses Spark as a library.

At the core (and part) of every Spark application is a Spark driver program, which creates a SparkSession object for you.

It provides the data processing code that Spark executes on the worker nodes.

A driver program can launch one or more jobs on a Spark cluster.

Program that produces the SparkContext, connecting to a given Spark Master.

Declares the actions and transformations on RDDs of data.


 */
package spark.driver;

public class SparkDriver {
}
