[project]
name = "pyspark-architecture"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pyspark<4.0.0",
]

[dependency-groups]
dev = [
    "pytest-sugar>=1.1.1",
    "pytest-xdist>=3.8.0",
]
