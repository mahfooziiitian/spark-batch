[project]
name = "pyspark-azure-storage"
version = "0.1.0"
description = "PySpark Azure Blob / ADLS Gen2 storage examples"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pyspark>=3.5.2",
]
