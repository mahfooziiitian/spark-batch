"""Pandas API on Spark (pyspark.pandas).

Modules
-------
- ``pandas_on_spark_dataframe`` — DataFrame creation and basic operations
- ``pandas_on_spark_ops_df``    — Cross-frame operations
- ``pandas_on_spark_options``   — Option management
- ``conversion/``               — Three-way DataFrame conversion
- ``missing_data``              — dropna, fillna, isna patterns
- ``groupby_agg``               — GroupBy and aggregation patterns
- ``string_ops``                — String operations via .str accessor
- ``io_ops``                    — Read/write CSV, Parquet, JSON
"""
