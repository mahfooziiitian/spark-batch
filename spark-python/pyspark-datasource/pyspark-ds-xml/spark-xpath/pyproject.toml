[project]
name = "spark-xpath"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pyspark<4.0.0",
]

[dependency-groups]
dev = [
    "pytest-mock>=3.15.1",
    "pytest-sugar>=1.1.1",
    "mkdocs>=1.6",
    "mkdocs-material>=9.5",
]
