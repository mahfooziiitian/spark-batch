extracts:
  extract:
    exec: read_api
    source:
      params:
        format: "api"
        # schema: "json"
        location: "http://127.0.0.1:8081/students"
        options:
          method: "GET"
          authentication:
            type: "basic"
            username: "admin"
            password: "secret123a"
          headers:
            Accept: "application/json"
          responseFormat: "json"
          retries:
            maxAttempts: 3
            delaySeconds: 5
          filepath: "api_basic_ds.json"
