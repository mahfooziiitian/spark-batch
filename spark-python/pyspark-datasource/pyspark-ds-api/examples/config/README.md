# Rest api using yaml

```text
[YAML CONFIG]
      |
   (read config)
      |
   (fetch data from API)
      |
   (convert to Spark DataFrame)
      |
   (Spark transformations/actions)
      |
   (save results / analysis)
```
