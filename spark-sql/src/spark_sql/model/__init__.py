"""Pydantic data models shared across ``spark_sql`` CLI tooling.

Currently holds :mod:`spark_sql.model.env` — environment-variable validation
and the :class:`~spark_sql.model.env.EnvConfig` model used to parameterize
per-environment dashboard/warehouse deployments.
"""
